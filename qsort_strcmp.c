#include <stdio.h>
#include <math.h>
#include <ctype.h>
#include <string.h>
#include <stdlib.h>

const int mod = 1e9 + 7;
typedef long long ll;

// neu muon a dung truoc thi tra ve -1 (so am)
// neu muon a dung sau thi tra ve 1 (so duong)
int cmp(const void *a, const void *b){
	
}

strcmp(x, y)
// so sanh 2 chuoi x va y
// tra ve so am (-1) neu x < y
// tra ve so duon (+1) neu x > y
int main(){


	return 0;
}

